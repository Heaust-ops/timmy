import {createContext} from 'react';

export const organiserCodeContext = createContext(null);
export const partyCodeContext = createContext(null);
export const memberCodeContext = createContext(null);
export const memberArrayContext = createContext(null);
export const pageContext = createContext(null);